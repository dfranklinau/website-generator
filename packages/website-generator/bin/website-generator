#!/usr/bin/env node
const args = process.argv.slice(2);
const websiteGenerator = require('../dist/index.js').default;

if (args.includes("--serve")) {
  websiteGenerator.serve();
} else {
  websiteGenerator.generate();
}
